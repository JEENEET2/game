# Source Plan

This folder is reserved for the C++ gameplay/engine code or Unreal C++ modules.

The local shell currently does not expose CMake or a C++ compiler on PATH, so the first commit focuses on design, project layout, and a concept preview. Once the toolchain is available, create either:

- an Unreal C++ project under `game/ValleyboundUE/`, or
- a native C++ prototype under this folder.
